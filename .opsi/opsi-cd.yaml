# Generated by opsi init. Review before committing.
version: 2
services:
  - key: api
    build:
      context: services/api
      dockerfile: services/api/Dockerfile
      platform: linux/amd64
    watchPaths: []
    sharedPaths:
      - .github/workflows/opsi-cd.yaml
      - .opsi/opsi-cd.yaml
    dependencies: []
    deploy:
      production:
        enabled: true
        branches:
          - main
      preview:
        enabled: false
        pullRequests: false
  - key: worker
    build:
      context: services/worker
      dockerfile: services/worker/Dockerfile
      platform: linux/amd64
    watchPaths: []
    sharedPaths:
      - .github/workflows/opsi-cd.yaml
      - .opsi/opsi-cd.yaml
    dependencies: []
    deploy:
      production:
        enabled: true
        branches:
          - main
      preview:
        enabled: false
        pullRequests: false
